# MongoDB Configuration
MONGODB_URL="mongodb+srv://<username>:<password>@cluster0.mongodb.net/hirehub?retryWrites=true&w=majority"
DATABASE_NAME="hirehub"

# JWT Auth Configuration
# You can generate a secure secret key using: openssl rand -hex 32 or python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY="replace_me_with_a_super_secret_key"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30
FRONTEND_ORIGINS="http://localhost:5173,http://127.0.0.1:5173"
